'use strict';

// Configuring the Articles module
angular.module('opt-items').run(['Menus',
	function(Menus) {
		// Set top bar menu items
//		Menus.addMenuItem('topbar', 'Opt items', 'opt-items', 'dropdown', '/opt-items(/create)?');
//		Menus.addSubMenuItem('topbar', 'opt-items', 'List Opt items', 'opt-items');
//		Menus.addSubMenuItem('topbar', 'opt-items', 'New Opt item', 'opt-items/create');
	}
]);